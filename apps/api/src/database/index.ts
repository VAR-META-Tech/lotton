import {
  Admin,
  AdminSession,
  LatestBlock,
  Network,
  NetworkToken,
  Token,
  TokenPrice,
  Transaction,
  User,
  UserSession,
} from './entities';

const entities = [
  Network,
  Token,
  LatestBlock,
  NetworkToken,
  Transaction,
  Admin,
  AdminSession,
  UserSession,
  User,
  TokenPrice,
];

export default entities;
